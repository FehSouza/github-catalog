import { Button, Container } from '@mui/material'

function App() {
  return (
    <Container
      sx={{
        maxWidth: 'lg',
        padding: '16px',
      }}
    >
      <Button variant="outlined">Hello World</Button>
    </Container>
  )
}

export default App
